# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Local Claude Code state (never publish machine-local settings)
.claude/settings.local.json

# Archives / build leftovers
*.zip
*.tgz
node_modules/

# Office temp/lock files
~$*

# Session state from the remember plugin. Belt and braces — .remember/ already
# carries its own .gitignore of "*", so this only matters if that file is lost.
.remember/
