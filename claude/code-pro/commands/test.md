---
description: Write unit tests mirroring the repo's test framework and style; must run and pass
---

Use the **test** skill on: $ARGUMENTS

Follow the skill exactly: mirror the repo's existing test framework/style/structure, cover success/failure/edge paths, run the tests until green, and finish with the fixed report (test cases created / run results / cannot be unit tested). Production code is untouchable except an explicitly approved testability seam.
