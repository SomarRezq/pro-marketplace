---
description: Fix a bug at its root cause with the smallest possible change + regression test
---

Use the **bug-fix** skill on: $ARGUMENTS

Follow the skill exactly: reproduce/understand, trace to the root cause (not the symptom), fix directly with the minimal change in repo style, add a regression test if similar code is tested, verify side effects, and finish with the fixed report (root cause / what changed and why / what was tested / manual verification steps).
