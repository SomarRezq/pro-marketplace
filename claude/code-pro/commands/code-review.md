---
description: Read-only severity-ranked review of a change/diff/branch/PR + suggested commit message
---

Use the **code-review** skill on: $ARGUMENTS (default: the current uncommitted changes / current branch vs its base)

Follow the skill exactly: understand the intent, delegate the deep pass to the code-reviewer agent, and return the fixed template — summary, Critical/Major/Minor findings with locations + why it matters + suggested fix, test coverage gaps, verdict, and the suggested commit message. Read-only — modify nothing.
