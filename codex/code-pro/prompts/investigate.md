---
description: Investigate existing code/behavior and produce visual workflow charts (read-only)
---

Use the **investigate** skill on: $ARGUMENTS

Follow the skill exactly: trace all related code paths and use cases (delegating to the investigator agent as the skill directs), produce the visual workflow charts with user-interaction/backend/DB boundaries and all success/failure paths, then the notes and worth-mentioning findings. Read-only — make no changes to any file.
